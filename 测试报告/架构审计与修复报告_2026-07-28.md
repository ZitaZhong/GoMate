# 架构审计与修复报告（DD-01~19 设计哲学修正）

**日期**：2026-07-28 · **起因**：产品负责人否决"抽取/理解/生成调用要便宜、要确定性、离线也要能跑"的设计哲学，要求系统**足够智能足够精准**——LLM/真实 API 为主路径，规则/确定性兜底仅作**显式诚实标注的降级**。
**方法**：4 路并行审计 DD-01~19 全部设计文档与对应实现，按统一标尺（主路径 + 规则冒充智能 + 错误/误导/低质输出 + 无降级标注）取证；随后分 6 包修复。

---

## 1. 审计发现与修复对照

### P0（用户可见错误输出）

| 发现 | 证据 | 修复 |
|---|---|---|
| 房间行程把长期展的展期当游览时长，出现「42450 分钟」 | `room_nodes.py` start_mismatch 只改 start 不透传处理 end；前端原样渲染分钟 | 后端：选中活动时将 end 截断（同日场次才保留，否则 start+3h 且不超共同时间窗，标 `end_estimated`）；前端：>12h 显示「全天/展期」，≥2h 显示约 X 小时 |

### P1（主路径错误/低质/语义架空）

| 发现 | 修复 |
|---|---|
| **证据红线**：search/community 来源未核实即标 `public_source_observed` 进入可信召回（DD-03：搜索仅找入口，未核实→unknown） | guard 闸一对 search/community 默认 `unknown`；深研 Phase 3 交叉验证补升级逻辑（第二独立来源核实→`public_source_observed`，官方白名单源→`official_source_confirmed`，只升不降，note 记录核实路径）。新增 3 条升级路径测试 |
| **语义检索静默造假**：哈希假向量冒名 bge-m3-v1；只配 LLM key 没配 embedding key 时每次 embed 白跑 30s 必败 HTTP | embed 熔断（连续 3 次失败进程内直接走哈希 + warning 日志）；`embedding_version` 按实际 provider 如实标注（`hashing-ngram-v1`）；`.env.example` 矛盾模型名合并并注明 use_real_models=false 仅限离线开发/CI |
| **Copilot 无状态 NLU**：抽取 prompt 只传当前一句，裸城市回答误判目的地（用户实测） | DD-15 重做：prompt 注入对话上下文（约束摘要+近 6 轮原文+未决槽位提示）；`plans.conversation` 落库（≤40 条）；裸城市上下文纠偏；`_summarize` 仅本轮真实变更字段说"改"；新增 7 条回归测试 |
| **ask_info 模板拼答案**：LLM 只抽关键词，答案是 SQL ILIKE 拼接 | 改为"检索候选行 + LLM 结合问题生成"（红线 prompt：事实只引用行内数据、没有就明说并给官方入口）；无 key 时显式标注"关键词匹配结果" |
| **DD-13 提醒系统空转**：payload 无文案、9 类中 5 类 fire_at 恒 None 永不触发、presale 不落库、bundle 缺 disclaimer | `build_reminders` 重写：每条提醒生成 title/body/action_url/disclaimer，presale fire_at 取自 DD-09 真实数据，flight_recheck/doc_check/72h/24h 按规则计算（算不出的两类注明原因不编造）；两版 bundle 补 `disclaimer` + `reminders_preview`（已单元验证：起售提醒 fire_at 正确产出） |
| **DD-09 前后端契约断裂**：后端 `total_min` 前端读 `total_minutes`，门到门数字永不渲染；`rail/air` 与 `rail_first/air_first` 不匹配；策略卡/起售前端零消费 | 前端类型与组件按后端实际产出对齐（补 run_min/buffer/effective_play_min）；MODE_LABELS 键统一；12306 起售 open_at<now 文案改「已起售，请直接购买」 |
| **静默默认值**：目的地缺省写死上海（非上海用户被静默推上海）；rerank query 默认废话串「周末不限 忌讳无」；score_city 预算维度恒 True | 删 310000 默认，目的地留空→按出发地同城推荐并写 warning；origins 解析失败写 warning；无偏好 query 留空跳过 rerank；budget_fit 按真实 budget_band（无数据则中性并注释说明） |
| **revision 时间修改**：「改到15:00」被静默按 +30 分钟平移全行程（`_CLOCK_RE` 死代码）；规则命中标 `degraded=False` | 接通绝对时刻解析（以核心活动为锚等幅平移）；解析失败显式提示「没理解要调整到什么时间」不再默认；规则命中统一 `degraded=True` 诚实标注 |
| **DD-16 记忆读写全断**：`extract_and_write` 无生产调用点，`UserContext` 无写入方 | 核实为**依赖用户账号体系（PRD P2）**，匿名 MVP 无锚点；不虚接假用户——DD-16 文档新增「当前实现状态」节如实标注，会话上下文由 plans.conversation 承载（已接通） |

### P2（设计债，已处理）

- dev 种子活动 5 条标最高可信态直写绕过 Guard → 全部降 `estimated` + note"样例数据"（防演示违反硬 KPI）
- 情报归一化无过去日期拦截（DB 实证 2007/2015/2017 年活动入库）→ `(end_at or start_at) < now` 直接 NormalizeError 入审核队列
- `upcoming_weekend` 从 dev 种子挪入 `domain/timeutil.py`（生产不再 import dev 模块）
- CityCard「本周末活动」硬编码 → 共享 `weekendLabel` helper 从 time_windows 派生
- 深研时间预算文档 25s vs 实现 300s → DD-17 回写 300s + SSE 心跳说明 + .env.example 同步
- **DD-04 §6.1「能用规则绝不调模型」原则** → 改写为「LLM/真实 API 为主路径，规则/确定性仅作显式标注的降级，禁止静默兜底」，docstring 同步

## 2. 判定为合规、明确不改的部分

- DD-03 闸三/六态证据护栏、交通票价余票禁编、"逐字段确认才是事实"（DD-10）
- DD-17 无 key 链路：跳过不污染库 + degraded 标记 + 空结果不编造
- DD-05 §9 / DD-06 §8 降级阶梯设计（文档本就要求诚实降级，是实现没跟上，已修）
- DD-12 贪心时间线求解器（文档明言确定性设计，不产对外事实）
- DD-18 转盘权重（+3/+1/-2 与 PRD §7.3.3 一致）、通勤 30 分钟默认（带 estimated+warning）

## 3. 验证汇总

| 项 | 结果 |
|---|---|
| 全量 pytest | **354 passed**（含新增 ~20 条：证据升级/熔断/契约/上下文/reminder/revision） |
| ruff | All checks passed |
| web-v2 build + lint | 通过 |
| live 多轮对话（真实 LLM） | 「下下周末去杭州→上海→我们两个人，预算人均一千五」：origins/party_size/budget 全部正确入槽、不重复追问、`plans.conversation` 存 6 条 |
| live 房间修改 | 「改到15:00」→ `itinerary_updated` 正确锚定（不再 +30min 平移） |
| 提醒组装单元验证 | 探索版 bundle 含 disclaimer；presale 起售提醒 fire_at 正确产出 |

## 4. 遗留（排期建议）

| 项 | 说明 |
|---|---|
| ~~能力缺失：点名锚点排路线~~ | **已补（本轮）**：新增 `design_itinerary` 意图 + `domain/route_design.py` 锚点路线求解（白天/晚间分型、就近接驳、用餐占位、estimated 标注、未命中锚点 unknown 保留）+ 前端 RoutePlanCard。实测：「既要动漫博物馆也要万兽之王演唱会」→ 博物馆 10:00 / 演唱会 19:00 路线卡（截图 `测试报告/route_plan_card.png`），10 条测试全过。过程中修掉三个自身实现缺陷：锚点匹配引号不归一化致同名顶替、消息自带约束未抽取致错城错窗、design_itinerary 误触发全量重跑 |
| DD-16 长期记忆接线 | 依赖用户账号体系（PRD P2），匿名阶段不接 |
| activity_booking / hotel_cancel_deadline fire_at | 需活动模型加 `booking_open_at`、回填加 `cancel_deadline`（DD-06/DD-10 数据面） |
| DD-09 rail_strategy/flight_strategy 策略卡组装 | 后端未组装此键（DD-09 §3.4/3.5 设计的查询策略卡），前端已就绪； presale 顶层数组渲染待加 |
| DD-10 截图 OCR | 多模态未接，当前等价于文本回填 |
| 房间餐饮 POI 注入 | `_collect_dining_pois` 未复用到房间侧，换餐厅无真候选（全程诚实占位） |
| 存量 DB 陈旧数据 | 2007/2015/2017 年旧活动需 expire_activities 节拍或一次性清理（normalize 只拦新写入） |
| `user_provided` 来源定级 | 闸一仍"有 URL→public_source_observed"，与 DD-03 字面有出入，建议单独评审 |
